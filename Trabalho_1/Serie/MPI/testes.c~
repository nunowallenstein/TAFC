#include<stdio.h>
#include<stdlib.h>

int main(int argc, char *argv[])
{
  int i,j;
  double *b, a[3][20];
  for (i=0;i<3;i++)
    for (j=0;j<20;j++)
      {
      a[i][j]=(i+1)*(j+1);
      // printf("%d\n",*(a[i] +j));
      }

   
  
  /* for (i=0;i<2;i++)
    for (j=0;j<2;j++)
      printf("%d\n",a[i][j]);
  */
   for(i=0;i<3;i++)
   b= a[0];
  
  
   // for(i=0;i<3;i++)
    for(j=0;j<20;j++)
      {    
     printf("%f\n",b[j]);
      }
      }
